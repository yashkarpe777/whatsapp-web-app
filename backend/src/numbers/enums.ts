export enum VirtualNumberStatus {
  ACTIVE = 'active',
  RESTRICTED = 'restricted',
  THROTTLED = 'throttled',
  BANNED = 'banned',
  DISCONNECTED = 'disconnected',
}

export enum VirtualNumberQuality {
  HIGH = 'high',
  MEDIUM = 'medium',
  LOW = 'low',
  UNKNOWN = 'unknown',
}
